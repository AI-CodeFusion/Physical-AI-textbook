---
title: ROS 2 Topics & Services
sidebar_label: ROS 2 Topics & Services
description: Learn about ROS 2 topics and services for communication
keywords: [ros2, topics, services, communication, publishers, subscribers]
---

import LearningObjectives from '@site/src/components/utils/LearningObjectives';
import SimulationComponent from '@site/src/components/simulations/SimulationComponent';
import AssessmentComponent from '@site/src/components/Assessment/AssessmentComponent';

# ROS 2 Topics & Services

## Overview
This lesson covers the primary communication patterns in ROS 2: topics (publish/subscribe) and services (request/reply).

<LearningObjectives objectives={[
  "Understand the publish/subscribe communication pattern",
  "Implement a publisher and subscriber in Python",
  "Understand the service/client communication pattern",
  "Implement a service and client in Python",
  "Compare topics vs services for different use cases"
]} />

## Topics and Publish/Subscribe

### Publisher
A publisher sends messages to a topic. Multiple publishers can send to the same topic.

### Subscriber
A subscriber receives messages from a topic. Multiple subscribers can listen to the same topic.

### Example: Publisher in Python
```python
import rclpy
from rclpy.node import Node
from std_msgs.msg import String

class MinimalPublisher(Node):

    def __init__(self):
        super().__init__('minimal_publisher')
        self.publisher_ = self.create_publisher(String, 'topic', 10)
        timer_period = 0.5  # seconds
        self.timer = self.create_timer(timer_period, self.timer_callback)
        self.i = 0

    def timer_callback(self):
        msg = String()
        msg.data = 'Hello World: %d' % self.i
        self.publisher_.publish(msg)
        self.get_logger().info('Publishing: "%s"' % msg.data)
        self.i += 1

def main(args=None):
    rclpy.init(args=args)
    minimal_publisher = MinimalPublisher()
    rclpy.spin(minimal_publisher)
    minimal_publisher.destroy_node()
    rclpy.shutdown()
```

### Example: Subscriber in Python
```python
import rclpy
from rclpy.node import Node
from std_msgs.msg import String

class MinimalSubscriber(Node):

    def __init__(self):
        super().__init__('minimal_subscriber')
        self.subscription = self.create_subscription(
            String,
            'topic',
            self.listener_callback,
            10)
        self.subscription  # prevent unused variable warning

    def listener_callback(self, msg):
        self.get_logger().info('I heard: "%s"' % msg.data)

def main(args=None):
    rclpy.init(args=args)
    minimal_subscriber = MinimalSubscriber()
    rclpy.spin(minimal_subscriber)
    minimal_subscriber.destroy_node()
    rclpy.shutdown()
```

## Services and Client/Server

### Service
A service performs an action and returns a response. It's a request/reply pattern.

### Client
A client sends a request to a service and waits for a response.

### Example: Service in Python
```python
from example_interfaces.srv import AddTwoInts
import rclpy
from rclpy.node import Node

class MinimalService(Node):

    def __init__(self):
        super().__init__('minimal_service')
        self.srv = self.create_service(AddTwoInts, 'add_two_ints', self.add_two_ints_callback)

    def add_two_ints_callback(self, request, response):
        response.sum = request.a + request.b
        self.get_logger().info('Incoming request\na: %d b: %d' % (request.a, request.b))
        return response

def main(args=None):
    rclpy.init(args=args)
    minimal_service = MinimalService()
    rclpy.spin(minimal_service)
    rclpy.shutdown()
```

### Example: Client in Python
```python
from example_interfaces.srv import AddTwoInts
import rclpy
from rclpy.node import Node

class MinimalClientAsync(Node):

    def __init__(self):
        super().__init__('minimal_client_async')
        self.cli = self.create_client(AddTwoInts, 'add_two_ints')
        while not self.cli.wait_for_service(timeout_sec=1.0):
            self.get_logger().info('service not available, waiting again...')
        self.req = AddTwoInts.Request()

    def send_request(self, a, b):
        self.req.a = a
        self.req.b = b
        self.future = self.cli.call_async(self.req)
        rclpy.spin_until_future_complete(self, self.future)
        return self.future.result()

def main(args=None):
    rclpy.init(args=args)
    minimal_client = MinimalClientAsync()
    response = minimal_client.send_request(1, 2)
    minimal_client.get_logger().info(
        'Result of add_two_ints: %d' % response.sum)
    minimal_client.destroy_node()
    rclpy.shutdown()
```

## When to Use Topics vs Services

Use **topics** when:
- You want to broadcast information
- Multiple nodes need the same data
- The information doesn't require a response
- The communication is asynchronous

Use **services** when:
- You need a response from a specific action
- The operation has side effects
- You need to ensure the operation completed
- The communication is synchronous

## Simulation
Experience ROS 2 communication patterns with our interactive simulation:

<SimulationComponent 
  simulationId="ros2-communication" 
  title="ROS 2 Communication Patterns Simulation" 
  description="Interact with nodes using both topics and services to understand their differences" 
/>

## Summary
Topics and services are the two primary communication patterns in ROS 2. Topics use a publish/subscribe model for asynchronous, decoupled communication, while services use a client/server model for synchronous request/reply communication.

## Knowledge Check

<AssessmentComponent
  type="multiple-choice"
  question="Which communication pattern would be most appropriate for broadcasting sensor data?"
  options={[
    "Services",
    "Topics", 
    "Actions",
    "Parameters"
  ]}
  correctAnswer={1}
  explanation="Topics are most appropriate for broadcasting sensor data as they allow multiple subscribers to receive the same data simultaneously."
/>

## Next Steps
Continue to the next lesson to learn about bridging Python agents with ROS controllers.